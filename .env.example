# 環境變量配置範例
# 複製此文件為 .env 並根據您的環境進行配置

# ===========================================
# 應用程序基本配置
# ===========================================

# 運行環境 (development, production, test)
NODE_ENV=production

# 後端服務端口
PORT=3001

# 前端服務 URL（用於 CORS 配置）
FRONTEND_URL=http://frontend:3000

# ===========================================
# 數據庫配置（可選 - 未來擴展使用）
# ===========================================

# PostgreSQL 數據庫連接
# DATABASE_URL=postgresql://username:password@localhost:5432/database_name

# MySQL 數據庫連接
# DATABASE_URL=mysql://username:password@localhost:3306/database_name

# MongoDB 連接
# MONGODB_URI=mongodb://localhost:27017/database_name

# ===========================================
# 認證和安全配置（可選 - 未來擴展使用）
# ===========================================

# JWT 密鑰（用於用戶認證）
# JWT_SECRET=your-super-secret-jwt-key-here-make-it-long-and-random

# JWT 過期時間
# JWT_EXPIRES_IN=7d

# 密碼加密輪數
# BCRYPT_ROUNDS=12

# ===========================================
# 第三方服務配置（可選）
# ===========================================

# Redis 配置（用於會話存儲或緩存）
# REDIS_URL=redis://localhost:6379

# 郵件服務配置
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# 文件上傳配置
# UPLOAD_MAX_SIZE=10485760  # 10MB in bytes
# UPLOAD_ALLOWED_TYPES=image/jpeg,image/png,image/gif,application/pdf

# ===========================================
# API 和外部服務配置（可選）
# ===========================================

# 外部 API 密鑰
# EXTERNAL_API_KEY=your-api-key-here
# EXTERNAL_API_URL=https://api.example.com

# Google OAuth 配置
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret

# GitHub OAuth 配置
# GITHUB_CLIENT_ID=your-github-client-id
# GITHUB_CLIENT_SECRET=your-github-client-secret

# ===========================================
# 日誌和監控配置
# ===========================================

# 日誌級別 (error, warn, info, debug)
LOG_LEVEL=info

# 是否啟用請求日誌
ENABLE_REQUEST_LOGGING=true

# 監控和分析
# SENTRY_DSN=https://your-sentry-dsn-here

# ===========================================
# Coolify 部署專用配置
# ===========================================

# 這些變量通常由 Coolify 自動設置，無需手動配置

# 應用程序 URL（由 Coolify 提供）
# APP_URL=https://your-app.coolify.io

# 數據庫 URL（如果使用 Coolify 的數據庫服務）
# DATABASE_URL=${DATABASE_URL}

# Redis URL（如果使用 Coolify 的 Redis 服務）
# REDIS_URL=${REDIS_URL}

# ===========================================
# 開發環境專用配置
# ===========================================

# 開發模式下的特殊配置
# DEBUG=true
# CORS_ORIGIN=http://localhost:3000
# API_BASE_URL=http://localhost:3001

# ===========================================
# 生產環境安全配置
# ===========================================

# 生產環境下的安全設置
# SECURE_COOKIES=true
# TRUST_PROXY=true
# RATE_LIMIT_WINDOW_MS=900000  # 15 minutes
# RATE_LIMIT_MAX_REQUESTS=100

# ===========================================
# 配置說明
# ===========================================

# 1. 將此文件複製為 .env
# 2. 取消註釋並配置您需要的變量
# 3. 確保 .env 文件已添加到 .gitignore 中
# 4. 在 Coolify 中設置相應的環境變量
# 5. 重啟應用程序以使配置生效

# 注意事項：
# - 不要將包含敏感信息的 .env 文件提交到版本控制系統
# - 在生產環境中使用強密碼和隨機密鑰
# - 定期更新和輪換敏感憑證
# - 使用 HTTPS 保護所有網絡通信